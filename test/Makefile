# -*-makefile-bsdmake-*-

bmake_topdir = ..

SUBDIR =	api
SUBDIR +=	parsing

.include <bsd.subdir.mk>

# set compiler and linker flags, especially additional warnings
# (here mostly to get the "docs" target)
#
.include "${.CURDIR}/${bmake_topdir}/Makefile.compiler"
